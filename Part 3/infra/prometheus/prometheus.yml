global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/alerts/feed-alerts.yml

scrape_configs:
  - job_name: "feedme-backend"
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: [ "backend:8080","host:docker.internal:8080" ]